name: meal_rate_calculator
description: A mobile app for dynamic meal rate calculation

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.9.0
  cloud_firestore: ^3.0.0
  firebase_auth: 3.11.2
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
